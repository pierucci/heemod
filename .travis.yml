language: r
sudo: false
cache: packages
r:
  - oldrel
  - release
  - devel

branches:
  only:
    - master
    - devel
    - /^hotfix\/.*$/
    - /^release\/.*$/

r_github_packages:		
  - hadley/devtools

r_packages:
  - covr
  - Hmisc

after_success:
  - Rscript -e 'library(covr); codecov()'
